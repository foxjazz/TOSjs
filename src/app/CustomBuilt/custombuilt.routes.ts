import { RouterConfig } from '@angular/router';

import { CustomBuiltComponent } from './custombuilt.component';

export const CustomBuiltRoutes: RouterConfig = [
  { path: 'custombuilt',  component: CustomBuiltComponent }
];
